package com.trading.telemetry;

/**
 * Stats that can be displayed on a metric's chart panel header.
 * Configure per metric via {@link TelemetryDashboard#chart(String...)}.
 */
public enum Stat {
    MIN, MAX, AVG
}
